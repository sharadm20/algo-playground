services:
  backend:
    build: ./backend
    expose:
      - 8001
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

  web:
    build: ./web
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
